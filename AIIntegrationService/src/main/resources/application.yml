server:
  port: 8084

spring:
  application:
    name: ai-integration-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_db
    username: postgres
    password: Umid/4709.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      Gemini:
      api-key: AIzaSyCPXPvVNnuaQFFr3i-DLFm_TDb3Bg1wBXI